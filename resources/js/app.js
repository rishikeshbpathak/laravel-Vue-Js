import './bootstrap';

import {createApp} from 'vue'; //imoprt this form vue 

import app from './component/a-app.vue';  //imoprt this form component folder with path

createApp(app).mount('#app') // call which file use s main file 

